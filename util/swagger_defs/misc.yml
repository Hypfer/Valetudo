components:
  schemas:
    SizeDTO:
      type: object
      properties:
        x:
          type: number
        y:
          type: number

    ScheduleTimeDTO:
      type: object
      properties:
        hour:
          type: integer
        minute:
          type: integer
