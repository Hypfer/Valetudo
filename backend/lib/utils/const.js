
/**
 *  @typedef {string} ImageFileFormat
 *  @enum {string}
 */
const IMAGE_FILE_FORMAT = Object.freeze({
    PNG: "png",
    JPG: "jpg",
});

module.exports = {
    IMAGE_FILE_FORMAT: IMAGE_FILE_FORMAT
};
